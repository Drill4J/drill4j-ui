version: '3.8'

services:
  nginx:
    image: nginx:latest
    ports:
      - "8085:8080"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ../build:/usr/share/nginx/html:ro
      # download latest build from https://github.com/Drill4J/drill4j-ui/releases
