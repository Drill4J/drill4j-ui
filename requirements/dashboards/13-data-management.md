# Data Management — Requirements Draft

**Status:** Draft — endpoints inventory only  
**Scope:** UI for deleting managed build / test-session data.

## API

```
DELETE /api/data-management/groups/{groupId}/apps/{appId}/builds/{buildId}
DELETE /api/data-management/groups/{groupId}/tests/sessions/{testSessionId}
```

## Notes

- Details (routes, entry points, confirmations, roles, UX) TBD.
